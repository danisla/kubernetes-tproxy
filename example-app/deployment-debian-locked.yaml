apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: debian-app-locked
  annotations:
    "initializer.kubernetes.io/tproxy": "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: debian-app-locked
  template:
    metadata:
      labels:
        app: debian-app-locked
        variant: locked
    spec:
      containers:
        - name: app
          image: danisla/example-app:debian
          imagePullPolicy: Always